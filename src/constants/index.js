// src/constants/index.js

const FIFTEEN_NINUTES = 15 * 60 * 1000;
const THIRTY_DAYS = 30 * 24 * 3600 * 1000;

export { FIFTEEN_NINUTES, THIRTY_DAYS };

export const SMTP = {
    SMTP_HOST: 'SMTP_HOST',
    SMTP_PORT: 'SMTP_PORT',
    SMTP_USER: 'SMTP_USER',
    SMTP_PASSWORD: 'SMTP_PASSWORD',
    SMTP_FROM: 'SMTP_FROM',
};
